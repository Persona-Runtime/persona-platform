# 승인 Sync 기록

`scripts/argo-preflight.sh <app>`가 매 실행마다 한 줄을 추가한다. 손으로 지우거나 순서를
바꾸지 않는다 — 롤백 명령이 "그 app의 직전 성공 SHA"를 이 표에서 찾는다.

이 파일은 Git에 커밋한다(`runbooks/`와 달리 gitignore 대상이 아니다) — 승인 이력은
저장소 밖(로컬 전용)에 두면 다른 사람·다른 세션이 볼 수 없다.

| 일시 | app | SHA | 실행자 | 결과 |
| --- | --- | --- | --- | --- |
