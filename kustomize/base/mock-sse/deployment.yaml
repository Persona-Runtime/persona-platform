apiVersion: apps/v1
kind: Deployment
metadata:
  name: persona-mock-sse
  labels:
    app.kubernetes.io/name: persona-mock-sse
    app.kubernetes.io/component: synthetic-sse-server
    app.kubernetes.io/part-of: persona-platform
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: persona-mock-sse
  template:
    metadata:
      labels:
        app.kubernetes.io/name: persona-mock-sse
        app.kubernetes.io/component: synthetic-sse-server
        app.kubernetes.io/part-of: persona-platform
    spec:
      automountServiceAccountToken: false
      # This Secret is created outside Git from a read-only GHCR package token.
      # It must exist before the private image can be pulled.
      imagePullSecrets:
        - name: persona-mock-sse-ghcr
      terminationGracePeriodSeconds: 15
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - k8s-worker1
                      - k8s-worker2
      containers:
        - name: mock-sse
          # This is deliberately not deployable until the GHCR linux/amd64 manifest
          # digest is recorded. Never substitute a tag (including "latest").
          image: ghcr.io/persona-runtime/persona-mock-sse@sha256:REPLACE_WITH_LINUX_AMD64_MANIFEST_DIGEST
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: MOCK_SSE_GRACE_PERIOD_SECONDS
              value: "5"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 2
            timeoutSeconds: 1
            failureThreshold: 15
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 10
            timeoutSeconds: 1
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            periodSeconds: 2
            timeoutSeconds: 1
            failureThreshold: 1
