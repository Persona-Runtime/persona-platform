apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# network-policy-fqdn.yaml.draft는 일부러 빠져 있다 — 모델 다운로드 호스트가 확정되지 않아
# placeholder가 들어 있고, 확장자와 resources 둘 다에서 제외해 실수로 적용될 길을 없앴다.
resources:
  - network-policy.yaml
