apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# namespace는 bootstrap/namespaces/persona-edge.yaml이 소유한다.
# 이 overlay에 Namespace를 넣지 않아, edge 컴포넌트 정리가 namespace 삭제로 번지지 않게 한다.
namespace: persona-edge
resources:
  - ../../../base/ddns
